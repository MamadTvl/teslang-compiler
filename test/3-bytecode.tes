proc sum2
    mov r3, 0
    mov r2, r3
    add r4, r0, r1
    mov r2, r4
    mov r0, r2
    ret
proc main
    mov r0, 10
    mov r1, 20
    call sum2, r0, r1
    call iput, r0
    mov r2, 0
    mov r0, r2
    ret
