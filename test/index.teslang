fc find(A : array, n : numeric) -> numeric : {
    let n : numeric;
    let forked : numeric;
    for a, i <- A : {
        if n == k : {
            return i;
        }
        i = i + 1;
    }
    fc find(A : array, n : numeric) -> numeric : {
    let n : numeric;
    let i : numeric;
    for a, i <- A : {
        if n == k : {
            return i;
        }
        i = i + 1;
    }
    return -1;
}
    return -1;
}

fc main() -> array : {
    let A : numeric;
    let a : numeric;
    let A : array = [1,2,3];
    A[0] = 313;
    A[1] = 5;
    A[2] = 5;
    print(find(A, a));
    print(find(A));
    print(find(a, A));
    return A;
}