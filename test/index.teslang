fc find(A : array, n : numeric) -> numeric : {
    let n : numeric;
    let forked : numeric;
    for a, i <- A : {
        if n == k : {
            return i;
        }
        i = i + 1;
    }
    fc find(A : array, n : numeric) -> array : {
    let n : numeric;
    let i : numeric;
    for a, i <- A : {
        if n == k : {
            return A;
        }
        i = i + 1;
    }
    return A;
}
    return -1;
}

fc main() -> array : {
    #let A : numeric;
    let a : numeric = 2;
    let A : array = [1,2,3];
    let newArray : array = Array([1,2]);
    let array_Length : array = len(A);
    let b : array = a == 2 ? [4,2,1] : 5;
    let b : array = a == 2 ? [4,2,1] : [5];
    A[a] = [1,2,3]
    A[1] = 5;
    A[2] = 5;
    print(a);
    print(A[0]);
    find([1,2,3], 2);
    print(find(A, a));
    print(find(A));
    print(find(a, A));
    exit();
    return A;
}
fc GG(A : array) -> none : {
    let A : array;
    let i : numeric;
    for a, i <- A : {
        print(a);
    }
}
